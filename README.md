
# AI-Визажист: Telegram Бот

Телеграм-бот, который консультирует по вопросам макияжа с помощью AI-модели (Llama 3.1). Бот запоминает историю диалога с каждым пользователем, сохраняя ее в базе данных PostgreSQL для поддержания контекста.

## 🚀 Основные возможности

-   **Персональные консультации:** Получайте советы по макияжу и уходу за кожей от AI.
-   **Контекстные диалоги:** Бот помнит предыдущие сообщения для более осмысленного общения.
-   **Управление историей:** Возможность сбросить историю диалога командой `/reset`.

## 🛠️ Требования

-   Python 3.8+
-   Docker
-   Аккаунт на [Hugging Face] с созданным Read-токеном.
-   Токен Telegram-бота, полученный от [@BotFather]

## ⚙️ Быстрый запуск

### 1. Клонирование репозитория

Склонируйте репозиторий на свой компьютер. Не забудьте заменить `YourUsername` на ваш ник на GitHub.

```bash
git clone https://github.com/YourUsername/ai-makeup-artist-bot.git
cd ai-makeup-artist-bot
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка токенов

Создайте файл `secrets.py` в корневой директории проекта и добавьте в него ваши API-токены:

```python
# secrets.py

secrets = {
    'BOT_API_TOKEN': 'сюда_вставьте_токен_вашего_бота',
    'HF_TOKEN': 'сюда_вставьте_ваш_read-токен_от_hugging_face'
}
```
> **Важно:** Файл `secrets.py` уже добавлен в `.gitignore`, чтобы ваши ключи не попали в публичный репозиторий.

### 4. Запуск базы данных

Запустите PostgreSQL в Docker-контейнере. Эта команда создаст и запустит контейнер в фоновом режиме.

```bash
docker run --name my-postgres-tgbot -e POSTGRES_PASSWORD=mysecretpassword -p 5433:5432 -d postgres:15-alpine
```
*Если контейнер уже был создан ранее, используйте `docker start my-postgres-tgbot` для его запуска.*

### 5. Запуск бота

```bash
python bot.py
```
После запуска бот готов к работе в Telegram.

## 🤖 Использование

-   **Начать диалог:** Просто отправьте боту любой вопрос о макияже.
-   **Сбросить историю:** Используйте команду `/reset` или нажмите на кнопку "RESET", чтобы бот "забыл" предыдущую переписку.

## 🏛️ Архитектура проекта

Проект имеет двухкомпонентную архитектуру для разделения ответственности (Separation of Concerns).

```
/
├── bot.py             # Фронтенд: обработка взаимодействий с Telegram API
├── llm_service.py     # Бэкенд: бизнес-логика, работа с AI и базой данных
├── secrets.py         # Хранит API-токены (не отслеживается Git)
├── requirements.txt   # Зависимости Python
└── README.md          # Этот файл
```

-   **`bot.py` (Фронтенд):** Этот скрипт отвечает за все взаимодействия с Telegram Bot API. Он получает сообщения от пользователей, перенаправляет их в бэкенд-сервис для обработки и отправляет сгенерированные AI-ответы обратно пользователю.

-   **`llm_service.py` (Бэкенд):** Ядро приложения, которое выступает в роли микросервиса и выполняет следующие задачи:
    -   Подключается к базе данных **PostgreSQL** для сохранения и извлечения истории диалога.
    -   Формирует и управляет контекстом для AI-модели.
    -   Взаимодействует с **Hugging Face API** для получения ответов от модели.

Такое разделение обеспечивает лучшую масштабируемость и упрощает поддержку. Например, можно заменить AI-модель или базу данных, не затрагивая логику работы самого бота.
```
